name: test
channels:
  - brsynth
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python >= 3.6
  - brs_libs == 0.2.0
  - cobra == 0.16
  - timeout-decorator == 0.4.1
#  - rdkit == 2020.03.6
