version: '3.8'

services:

  tests:
    image: ${PACKAGE}-tests
    build:
      context: ../../..
      args:
        PKG: ${PACKAGE}
        HOME: ${HOMEDIR}
        IMAGE: ${BASE_IMAGE}
      dockerfile: tests/pytest/docker/Dockerfile
    entrypoint: conda run -n test
    command: pytest --verbose
